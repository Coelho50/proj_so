Pra rodar o jogo em um ambiente linux (ubuntu) basta:

1: Instalar as bibliotecas necessárias
`sudo apt update && sudo apt install libncurses5-dev libncursesw5-dev`

2: Usar make
`make clean`
`make`

3: executar o jogo
`./jogo`